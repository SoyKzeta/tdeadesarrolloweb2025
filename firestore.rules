rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Permitir acceso completo en modo desarrollo
    // ADVERTENCIA: Estas reglas son peligrosas en producción
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 